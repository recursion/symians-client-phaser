// import * as World from '../world/world';
// import { not, contains } from 'ramda';

export const Selection = () => (
    {
        selection: [],
        selecting: false,
        start: null,
        current: null
    }
);
export const startSelection = (coords, selection) => {
};
export const stopSelection = (coords, selection) => {
};
export const updateSelection = (coords, selection) => {
};
